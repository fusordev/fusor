{
    let blockValue = 1;
    const blockConstant = blockValue;
    blockConstant;
}

for (let index = 0; index < 1; index += 1) {
    const iterationValue = index;
    iterationValue;
}

try {
    throw 1;
} catch (caught) {
    let recovered = caught;
    recovered;
}
