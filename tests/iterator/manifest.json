{
  "schema": 1,
  "quickjs_release": "2026-06-04",
  "cases": [
    {
      "id": "array-spread-preserves-elements",
      "covers": ["array-spread"],
      "body": "let value = [1, ...[2, 3], 4];\nreturn \"\" + value[0] + value[1] + value[2] + value[3] + \":\" + value.length;",
      "expect": {"kind": "string", "value": "1234:4"}
    },
    {
      "id": "array-spread-densifies-elisions",
      "covers": ["array-spread-elision"],
      "body": "let value = [, ...[, 2], ,];\nreturn (value[0] === void 0) + \":\" + (value[1] === void 0) + \":\" + value[2] + \":\" + (value[3] === void 0) + \":\" + value.length;",
      "expect": {"kind": "string", "value": "true:true:2:true:4"}
    },
    {
      "id": "array-spread-evaluates-left-to-right",
      "covers": ["array-spread-evaluation-order"],
      "body": "let log = \"\";\nfunction mark(value) { log = log + value; return value; }\nlet result = [mark(\"a\"), ...[mark(\"b\")], mark(\"c\")];\nreturn log + \":\" + result[0] + result[1] + result[2];",
      "expect": {"kind": "string", "value": "abc:abc"}
    },
    {
      "id": "array-spread-supports-multiple-sources",
      "covers": ["array-spread-multiple"],
      "body": "let value = [...[1], 2, ...[3, 4]];\nreturn \"\" + value[0] + value[1] + value[2] + value[3] + \":\" + value.length;",
      "expect": {"kind": "string", "value": "1234:4"}
    },
    {
      "id": "empty-trailing-spread-preserves-hole-length",
      "covers": ["array-spread-pending-hole-length"],
      "body": "let value = [1, , ...[]];\nreturn value.length + \":\" + (value[1] === void 0);",
      "expect": {"kind": "string", "value": "2:true"}
    },
    {
      "id": "string-spread-uses-code-points",
      "covers": ["array-spread-string", "array-spread-string-code-point"],
      "body": "let ascii = [...\"ab\"];\nlet astral = [...\"A😀B\"];\nreturn ascii[0] + ascii[1] + \":\" + ascii.length + \"|\" + astral.length + \":\" + astral[0].length + \":\" + astral[1].length + \":\" + astral[2].length;",
      "expect": {"kind": "string", "value": "ab:2|3:1:2:1"}
    },
    {
      "id": "custom-iterator-orders-and-retains-methods",
      "covers": ["iterator-method-receiver", "iterator-next-retained", "iterator-done-before-value", "iterator-done-skips-value"],
      "body": "let log = \"\";\nlet iterable = {};\nlet iterator = {};\nlet step = 0;\nfunction retained() {\n  log = log + \"n\";\n  step = step + 1;\n  iterator.next = function reread() { throw new Error(\"next was reread\"); };\n  if (step === 1) return { get done() { log = log + \"d\"; return false; }, get value() { log = log + \"v\"; return \"x\"; } };\n  return { get done() { log = log + \"D\"; return true; }, get value() { log = log + \"V\"; return \"wrong\"; } };\n}\niterator.next = retained;\niterable[Symbol.iterator] = function iteratorMethod() { log = log + (this === iterable ? \"i\" : \"I\"); return iterator; };\nlet value = [...iterable];\nreturn log + \":\" + value[0] + \":\" + value.length;",
      "expect": {"kind": "string", "value": "indvnD:x:1"}
    },
    {
      "id": "append-performs-quickjs-method-probe",
      "covers": ["iterator-method-double-lookup"],
      "body": "let log = \"\";\nlet iterator = { next: function next() { return {done: true}; } };\nlet iterable = { get [Symbol.iterator]() { log = log + \"g\"; return function iteratorMethod() { log = log + \"c\"; return iterator; }; } };\n[...iterable];\nreturn log;",
      "expect": {"kind": "string", "value": "ggc"}
    },
    {
      "id": "normal-exhaustion-does-not-close",
      "covers": ["iterator-close"],
      "body": "let closed = 0;\nlet iterator = { next: function next() { return {done: true}; }, return: function close() { closed = closed + 1; return {}; } };\nlet iterable = {};\niterable[Symbol.iterator] = function iteratorMethod() { return iterator; };\n[...iterable];\nreturn closed;",
      "expect": {"kind": "number", "bits": "0x0000000000000000"}
    },
    {
      "id": "noncallable-iterator-method-is-rejected",
      "covers": ["iterator-noncallable-method"],
      "body": "let iterable = {};\niterable[Symbol.iterator] = 1;\nreturn [...iterable];",
      "expect": {"kind": "throw", "name": "TypeError", "message": "value is not iterable"}
    },
    {
      "id": "iterator-method-must-return-object",
      "covers": ["iterator-nonobject"],
      "body": "let iterable = {};\niterable[Symbol.iterator] = function iteratorMethod() { return 1; };\nreturn [...iterable];",
      "expect": {"kind": "throw", "name": "TypeError", "message": "not an object"}
    },
    {
      "id": "next-must-return-object",
      "covers": ["iterator-result-nonobject"],
      "body": "let iterable = {};\niterable[Symbol.iterator] = function iteratorMethod() { return { next: function next() { return 1; } }; };\nreturn [...iterable];",
      "expect": {"kind": "throw", "name": "TypeError", "message": "iterator must return an object"}
    },
    {
      "id": "iterator-close-preserves-next-exception",
      "covers": ["iterator-close-preserves-exception"],
      "body": "let log = \"\";\nlet step = 0;\nlet iterator = {};\niterator.next = function next() { log = log + \"n\"; step = step + 1; if (step === 1) return {done: false, value: \"x\"}; throw \"original\"; };\niterator.return = function close() { log = log + \"r\"; throw \"close\"; };\nlet iterable = {};\niterable[Symbol.iterator] = function iteratorMethod() { return iterator; };\ntry { [...iterable]; } catch (error) { return log + \":\" + error; }",
      "expect": {"kind": "string", "value": "nnr:original"}
    },
    {
      "id": "iterator-close-preserves-value-exception",
      "covers": ["iterator-close-preserves-exception"],
      "body": "let log = \"\";\nlet iterator = {};\niterator.next = function next() { return { get done() { log = log + \"d\"; return false; }, get value() { log = log + \"v\"; throw \"original\"; } }; };\niterator.return = function close() { log = log + \"r\"; return 1; };\nlet iterable = {};\niterable[Symbol.iterator] = function iteratorMethod() { return iterator; };\ntry { [...iterable]; } catch (error) { return log + \":\" + error; }",
      "expect": {"kind": "string", "value": "dvr:original"}
    },
    {
      "id": "initial-next-getter-failure-does-not-close",
      "covers": ["iterator-next-getter", "iterator-close-boundary"],
      "body": "let log = \"\";\nlet iterator = { get next() { log = log + \"g\"; throw \"original\"; }, return: function close() { log = log + \"r\"; return {}; } };\nlet iterable = {};\niterable[Symbol.iterator] = function iteratorMethod() { return iterator; };\ntry { [...iterable]; } catch (error) { return log + \":\" + error; }",
      "expect": {"kind": "string", "value": "g:original"}
    },
    {
      "id": "nested-abrupt-iteration-closes-inner-and-outer",
      "covers": ["iterator-close-nested", "iterator-close-order"],
      "body": "let log = \"\";\nlet inner = { next: function next() { throw \"original\"; }, return: function close() { log = log + \"i\"; return {}; } };\ninner[Symbol.iterator] = function iteratorMethod() { return inner; };\nlet outer = { next: function next() { [...inner]; }, return: function close() { log = log + \"o\"; return {}; } };\nouter[Symbol.iterator] = function iteratorMethod() { return outer; };\ntry { [...outer]; } catch (error) { return error + \":\" + log; }",
      "expect": {"kind": "string", "value": "original:io"}
    },
    {
      "id": "nested-inner-close-reaches-catch-before-outer-close",
      "covers": ["iterator-close-catch-boundary"],
      "body": "let log = \"\";\nlet inner = { next: function next() { throw \"original\"; }, return: function close() { log = log + \"i\"; return {}; } };\ninner[Symbol.iterator] = function iteratorMethod() { return inner; };\nlet outer = { next: function next() { try { [...inner]; } catch (error) { log = log + \"c\"; return {done: true}; } }, return: function close() { log = log + \"o\"; return {}; } };\nouter[Symbol.iterator] = function iteratorMethod() { return outer; };\n[...outer];\nreturn log;",
      "expect": {"kind": "string", "value": "ic"}
    },
    {
      "id": "for-of-array-observes-holes-as-undefined",
      "covers": ["for-of-array"],
      "body": "let log = \"\";\nfor (const value of [1, , 3]) {\n  log = log + (value === void 0 ? \"u\" : value);\n}\nreturn log;",
      "expect": {"kind": "string", "value": "1u3"}
    },
    {
      "id": "for-of-string-uses-code-points",
      "covers": ["for-of-string-code-point"],
      "body": "let log = \"\";\nlet count = 0;\nfor (const value of \"A😀B\") {\n  log = log + value.length;\n  count = count + 1;\n}\nreturn log + \":\" + count;",
      "expect": {"kind": "string", "value": "121:3"}
    },
    {
      "id": "for-of-null-acquisition-names-symbol-iterator",
      "covers": ["for-of-null-acquisition"],
      "body": "for (const value of null) {}",
      "expect": {"kind": "throw", "name": "TypeError", "message": "cannot read property 'Symbol.iterator' of null"}
    },
    {
      "id": "for-of-undefined-acquisition-names-symbol-iterator",
      "covers": ["for-of-undefined-acquisition"],
      "body": "for (const value of void 0) {}",
      "expect": {"kind": "throw", "name": "TypeError", "message": "cannot read property 'Symbol.iterator' of undefined"}
    },
    {
      "id": "for-of-custom-iterator-retains-receivers-and-order",
      "covers": ["for-of-custom-iterator", "for-of-iterator-method-receiver", "for-of-next-receiver", "for-of-next-retained", "for-of-step-order", "for-of-done-skips-value"],
      "body": "let log = \"\";\nlet iterator = {};\nlet step = 0;\nfunction retainedNext() {\n  log = log + (this === iterator ? \"n\" : \"N\");\n  step = step + 1;\n  iterator.next = function reread() { throw \"next was reread\"; };\n  if (step === 1) {\n    return { get done() { log = log + \"d\"; return false; }, get value() { log = log + \"v\"; return \"x\"; } };\n  }\n  return { get done() { log = log + \"D\"; return true; }, get value() { log = log + \"V\"; return \"wrong\"; } };\n}\niterator.next = retainedNext;\nlet iterable = { get [Symbol.iterator]() { log = log + \"g\"; return function iteratorMethod() { log = log + (this === iterable ? \"i\" : \"I\"); return iterator; }; } };\nlet value = \"\";\nfor (const item of iterable) value = value + item;\nreturn log + \":\" + value;",
      "expect": {"kind": "string", "value": "gindvnD:x"}
    },
    {
      "id": "for-of-natural-exhaustion-does-not-close",
      "covers": ["for-of-natural-exhaustion"],
      "body": "let log = \"\";\nlet step = 0;\nlet iterator = { next: function next() { log = log + \"n\"; step = step + 1; return step === 1 ? {done: false, value: \"x\"} : {done: true}; }, return: function close() { log = log + \"r\"; return {}; } };\niterator[Symbol.iterator] = function iteratorMethod() { return iterator; };\nfor (const value of iterator) log = log + value;\nreturn log;",
      "expect": {"kind": "string", "value": "nxn"}
    },
    {
      "id": "for-of-var-head-remains-visible",
      "covers": ["for-of-head-var"],
      "body": "function readLast() {\n  for (var value of [1, 2]) {}\n  return value;\n}\nreturn readLast();",
      "expect": {"kind": "number", "bits": "0x4000000000000000"}
    },
    {
      "id": "for-of-let-head-is-loop-scoped",
      "covers": ["for-of-head-let"],
      "body": "let value = \"outer\";\nlet log = \"\";\nfor (let value of [1, 2]) log = log + value;\nreturn log + \":\" + value;",
      "expect": {"kind": "string", "value": "12:outer"}
    },
    {
      "id": "for-of-const-head-initializes-each-iteration",
      "covers": ["for-of-head-const"],
      "body": "let sum = 0;\nfor (const value of [1, 2]) sum = sum + value;\nreturn sum;",
      "expect": {"kind": "number", "bits": "0x4008000000000000"}
    },
    {
      "id": "for-of-identifier-head-assigns-existing-binding",
      "covers": ["for-of-head-identifier"],
      "body": "let value = 0;\nfor (value of [1, 2]) {}\nreturn value;",
      "expect": {"kind": "number", "bits": "0x4000000000000000"}
    },
    {
      "id": "for-of-static-member-head-assigns-property",
      "covers": ["for-of-head-static-member"],
      "body": "let target = {value: 0};\nfor (target.value of [1, 2]) {}\nreturn target.value;",
      "expect": {"kind": "number", "bits": "0x4000000000000000"}
    },
    {
      "id": "for-of-computed-head-evaluates-after-each-step",
      "covers": ["for-of-head-computed-member"],
      "body": "let log = \"\";\nlet step = 0;\nlet iterator = { next: function next() { step = step + 1; log = log + \"n\"; return step > 2 ? {done: true} : {done: false, value: step}; } };\niterator[Symbol.iterator] = function iteratorMethod() { return iterator; };\nlet target = {};\nfunction key() { log = log + \"k\"; return \"value\"; }\nfor (target[key()] of iterator) log = log + \"b\";\nreturn log + \":\" + target.value;",
      "expect": {"kind": "string", "value": "nkbnkbn:2"}
    },
    {
      "id": "for-of-lexical-captures-are-fresh",
      "covers": ["for-of-capture-freshness"],
      "body": "let first;\nlet second;\nfor (const value of [1, 2]) {\n  if (first === void 0) first = function readFirst() { return value; };\n  else second = function readSecond() { return value; };\n}\nreturn first() + \":\" + second();",
      "expect": {"kind": "string", "value": "1:2"}
    },
    {
      "id": "for-of-same-loop-continue-does-not-close",
      "covers": ["for-of-continue-no-close"],
      "body": "let closed = 0;\nlet step = 0;\nlet iterator = { next: function next() { step = step + 1; return step <= 2 ? {done: false, value: step} : {done: true}; }, return: function close() { closed = closed + 1; return {}; } };\niterator[Symbol.iterator] = function iteratorMethod() { return iterator; };\nfor (const value of iterator) { if (value < 3) continue; }\nreturn closed;",
      "expect": {"kind": "number", "bits": "0x0000000000000000"}
    },
    {
      "id": "for-of-break-performs-normal-close",
      "covers": ["for-of-break-close"],
      "body": "let log = \"\";\nlet iterator = { next: function next() { return {done: false, value: 1}; }, return: function close() { log = log + (this === iterator ? \"r\" : \"R\"); return {}; } };\niterator[Symbol.iterator] = function iteratorMethod() { return iterator; };\nfor (const value of iterator) { log = log + \"b\"; break; }\nreturn log;",
      "expect": {"kind": "string", "value": "br"}
    },
    {
      "id": "for-of-return-preserves-value-after-close",
      "covers": ["for-of-return-close"],
      "body": "let log = \"\";\nlet iterator = { next: function next() { return {done: false, value: 1}; }, return: function close() { log = log + \"r\"; return {}; } };\niterator[Symbol.iterator] = function iteratorMethod() { return iterator; };\nfunction take() { for (const value of iterator) return value + 1; return 0; }\nreturn take() + \":\" + log;",
      "expect": {"kind": "string", "value": "2:r"}
    },
    {
      "id": "for-of-labeled-break-closes-inner-then-outer",
      "covers": ["for-of-labeled-close-order"],
      "body": "let log = \"\";\nfunction makeIterator(name) {\n  let iterator = { next: function next() { return {done: false, value: 1}; }, return: function close() { log = log + name; return {}; } };\n  iterator[Symbol.iterator] = function iteratorMethod() { return iterator; };\n  return iterator;\n}\nouter: for (const outerValue of makeIterator(\"o\")) {\n  for (const innerValue of makeIterator(\"i\")) break outer;\n}\nreturn log;",
      "expect": {"kind": "string", "value": "io"}
    },
    {
      "id": "for-of-body-throw-closes-and-preserves-original",
      "covers": ["for-of-body-throw-close", "for-of-close-original-wins"],
      "body": "let log = \"\";\nlet iterator = { next: function next() { return {done: false, value: 1}; }, return: function close() { log = log + \"r\"; throw \"close\"; } };\niterator[Symbol.iterator] = function iteratorMethod() { return iterator; };\ntry {\n  for (const value of iterator) { log = log + \"b\"; throw \"body\"; }\n} catch (error) {\n  return error + \":\" + log;\n}",
      "expect": {"kind": "string", "value": "body:br"}
    },
    {
      "id": "for-of-binding-throw-closes-and-preserves-original",
      "covers": ["for-of-binding-throw-close", "for-of-close-original-wins"],
      "body": "let log = \"\";\nlet target = { set value(nextValue) { log = log + \"s\"; throw \"binding\"; } };\nlet iterator = { next: function next() { return {done: false, value: 1}; }, return: function close() { log = log + \"r\"; throw \"close\"; } };\niterator[Symbol.iterator] = function iteratorMethod() { return iterator; };\ntry {\n  for (target.value of iterator) {}\n} catch (error) {\n  return error + \":\" + log;\n}",
      "expect": {"kind": "string", "value": "binding:sr"}
    },
    {
      "id": "for-of-next-error-does-not-close",
      "covers": ["for-of-next-error-no-close"],
      "body": "let log = \"\";\nlet iterator = { next: function next() { throw \"next\"; }, return: function close() { log = log + \"r\"; return {}; } };\niterator[Symbol.iterator] = function iteratorMethod() { return iterator; };\ntry { for (const value of iterator) {} } catch (error) { return error + \":\" + log; }",
      "expect": {"kind": "string", "value": "next:"}
    },
    {
      "id": "for-of-done-error-does-not-close",
      "covers": ["for-of-done-error-no-close"],
      "body": "let log = \"\";\nlet iterator = { next: function next() { return { get done() { throw \"done\"; } }; }, return: function close() { log = log + \"r\"; return {}; } };\niterator[Symbol.iterator] = function iteratorMethod() { return iterator; };\ntry { for (const value of iterator) {} } catch (error) { return error + \":\" + log; }",
      "expect": {"kind": "string", "value": "done:"}
    },
    {
      "id": "for-of-value-error-does-not-close",
      "covers": ["for-of-value-error-no-close"],
      "body": "let log = \"\";\nlet iterator = { next: function next() { return { get done() { log = log + \"d\"; return false; }, get value() { throw \"value\"; } }; }, return: function close() { log = log + \"r\"; return {}; } };\niterator[Symbol.iterator] = function iteratorMethod() { return iterator; };\ntry { for (const value of iterator) {} } catch (error) { return error + \":\" + log; }",
      "expect": {"kind": "string", "value": "value:d"}
    },
    {
      "id": "for-of-normal-close-requires-object-result",
      "covers": ["for-of-close-nonobject-result"],
      "body": "let iterator = { next: function next() { return {done: false, value: 1}; }, return: function close() { return 1; } };\niterator[Symbol.iterator] = function iteratorMethod() { return iterator; };\nfor (const value of iterator) break;",
      "expect": {"kind": "throw", "name": "TypeError", "message": "not an object"}
    }
  ]
}
