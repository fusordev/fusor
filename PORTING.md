# Porting roadmap

## Target and boundaries

Port the observable JavaScript and host behavior of [QuickJS 2026-06-04](UPSTREAM.md) to safe, pure Rust: ES2025 Script/Module, explicitly admitted later ECMA-262 features, and the targeted Annex B web-compatibility subset listed below. ECMA-262 is normative; pinned QuickJS is the compatibility/diagnostic target; Oxc is parsing and semantic analysis only. QuickJS and Node are differential oracles, never runtime dependencies.

This is a source-level port, not a C API or byte-layout clone. The core links neither C nor C++. Unsupported source semantics reject before execution, and only whole-graph [`VerifiedBytecode`](BYTECODE_VERIFIER.md) executes. Focused tests prove only their named behavior.

| Crate | Owns |
| --- | --- |
| `fusor-diagnostics` | sources, diagnostics, spans, source maps |
| `fusor-frontend` | published Oxc records and parsing goals |
| `fusor-bytecode` | instructions, codec, verifier, debug data |
| `fusor-compiler` | iterative Oxc-to-verified-bytecode lowering |
| `fusor-regexp` | bounded ES RegExp grammar and UTF-16 execution |
| `fusor-runtime` | values, heap, realms, VM, built-ins, limits |
| `fusor` | embedding and tool facade |

Tooling, inspector, Wasm, N-API, TypeScript erasure, Serde conversion, and general Tokio driving remain optional layers. The runtime uses Tokio `rt`/`sync`/`time` only to deliver finite `Atomics.waitAsync` deadline signals; it retains waiter, Promise, and job ordering. See [ARCHITECTURE.md](ARCHITECTURE.md) for the ownership boundaries.

## Ordered status

Finish frontend/diagnostics and language/compiler/execution before broad Test262 alignment. Checked items have focused regressions.

### Frontend and diagnostics

- [x] Published Oxc `0.142.0` is directly pinned; Script, Module, strict/async-global, and dynamic-Function goals retain owned source, binding, and module records. RegExp literal errors use `fusor-regexp`.
- [x] The compatibility ledger covers admitted grammar and reachable diagnostics.
- [x] Registered sources resolve UTF-8 spans through bounded, cycle-checked source-map v3 chains with generated fallbacks. `evaluate_registered_script` preserves source identity across frontend, compiler, install, and runtime failures; stable diagnostics render through Miette, with verified JavaScript caller frames as independently sourced related diagnostics and exact source-text validation before mapping.

### Compiler, bytecode, and execution

- [x] Typed opcode metadata, bounded codec/disassembly, resource certificates, total decoding, and whole-child-graph verification. Lexical-environment and object/class/array provenance certificates are isolated verifier modules; function-instantiation prefixes are certified independently from direct-eval parameter/body boundaries. Abrupt-stack certificates preserve enclosing for-in and exact finalizer-continuation pairs only when a nested catch or complete inner for-of handler owns the next unwind; malformed pairs and crossed markers remain fail closed. Finalizer loop backedges require the target-bound active pair, while marker-free disconnected scaffolding remains inert. Compiler `gosub` joins defer nominal differences caused solely by certified variable-width `nip_catch` to typed whole-graph equality; runtime finalizer/catch transfers renormalize the structural correction. The six affected report files pass 12/12 modes. Limits bound semantic work, not implementation-size snapshots. Raw or serialized bytecode cannot execute; generator terminals may abandon only verifier-accounted expression state retained across suspension.
- [x] Iterative lowering/execution for the admitted ordinary profile: closures, control flow, calls/spread, destructuring, exceptions, synchronous/asynchronous lexical arrows, functions and generators, `yield*`, templates, static/computed/shorthand/spread object data properties and methods/accessors, optional chains, construction, and `new.target`. Defaulted binding identifiers apply `NamedEvaluation` to anonymous ordinary/async/generator/arrow/class definitions, including the class-specific formal-parameter path; shorthand object assignment defaults name anonymous classes before the store, closing two report files (4/4 modes). With parameter expressions, the parameter Environment Record owns `arguments` even when an Oxc-resolved body lexical declaration has the same name; ordinary/generator declaration/expression report cases pass 4/4, with the four adjacent function-declaration-shadowing cases retained at 4/4. Object spread uses certified `CopyDataProperties`: nullish sources are no-ops, primitives box, and enumerable string/Symbol own keys preserve source order. Generator abrupt return carries scoped assignment-destructuring and `for-of`-head iterator markers; certified variable-width `nip_catch` discards expression temporaries and closes active or exhausted nested records. All 44 affected report files pass 88/88 modes at pinned Test262. Disconnected source tails remain frontend-validated, then ordinary dead instructions and mappings are excised while verifier-required binding and exception structure remains authoritative. Generated try/finally rethrows apply the source-throw crossed-marker cleanup while complete inner for-of records retain handler-owned unwind; the three affected report files pass 6/6 modes. Computed compound/logical assignments and updates use verified `get_array_el3`, preserving one converted PropertyKey after the nullish check. Captured nonlogical compound identifiers use a verifier-contained, GC-traced `make_var_ref_ref`/`get_ref_value`/`put_ref_value` transaction, so RHS direct eval cannot retarget the original Environment Record. Simple identifier assignment uses the same transaction for captured cells and retains whether a realm-global name resolved before its RHS; its three report modes pass, and `language/identifier-resolution` is 22/22. The compound fixes close all 33 supplied report modes; that subtree passes 786/786.
- [x] Statement `for await...of` uses `GetAsyncIterator` with the async-from-sync fallback, awaits each `next()` result before reading `done` then `value`, leaves step/await/result-property failures unclosed, and performs awaited `AsyncIteratorClose` for abrupt assignment/body exits and normal loop transfers. The typed verifier binds async start/step/await/extraction to one record and rejects mixed or omitted stages. Both report-failing ASI files pass 2/2 at pinned Test262; async-host execution modes remain policy-skipped, so close/error ordering is covered by focused VM regressions rather than a subtree-rate claim.
- [~] Backend optimization includes verifier-preserving symbolic CFG branch threading, sparse conditional constant propagation over verified stack facts and exact primitive constant-pool truthiness, and ordinary unreachable instruction/block excision. Reassembly rebuilds exact source/debug mappings, drops dead statement/eval metadata, remaps live anchors and initializer boundaries, and re-verifies each function plus the whole graph. Dead lexical/function-initializer anchors, one required loop-activation backedge certificate, and complete catch/gosub/finalizer components remain only where the unchanged whole-graph verifier requires them. Four exact truthiness/finally Test262 files pass 8/8 modes at `be13516fb6441b950ba8a3df97eb34062c186972`; inline caches and guarded intrinsic specialization remain pending.
- [x] ECMAScript proper tail calls are verified terminal transfers for strict ordinary functions, arrows, methods, and constructors. Tail-position analysis covers the specified parenthesized, comma-final, conditional-arm, logical-RHS, optional-chain-final, tagged-template, and statement shapes; direct, method, spread, and identity-checked `eval` calls use `tail_call`, `tail_call_method`, `tail_apply`, `tail_eval`, or `tail_apply_eval`. Final authority rejects sloppy/async/generator claims, nonzero spread modes, and live catch/for-of records. The VM replaces the caller while preserving receivers, native/bound/Proxy paths, outer continuations, dynamic roots, and base/derived constructor completion. Error stack capture uses a retained native-operation origin only while it names the newest live frame; after `PrepareForTailCall` elides that frame, the parked surviving location is authoritative (34/36 non-Temporal report modes close; two Set-method files expose independent failures). At pinned Test262 `be13516fb6441b950ba8a3df97eb34062c186972`, all 34 admitted `tail-call-optimization` cases pass across 35 files; the remaining revoked-Proxy Realm case is the existing `$262.createRealm` host-API skip. Focused regressions include 100,000 transfers under a one-frame limit; no full-suite rerun is claimed.
- [x] Global Script evaluation preserves realm `var`, lexical bindings, TDZ, declaration conflicts, and source identity across evaluations.
- [~] Classes: global lexical declarations; named base/derived forms; `extends null`; explicit/default constructors; `super(...)` including spread; public static/instance fields, methods/accessors, computed keys, and static blocks. Direct/computed `super` preserves `this` and spec order through reads, optional/spread calls, writes, updates, destructuring/iteration targets, and delete errors. One hidden strict method owns instance elements; ordinary and direct-eval `super()` (including nested eval, lexical arrows, and spread) invoke its single certified capture only after binding the shared derived `this`, with abrupt and repeated-call ordering preserved. Named-class cells retain TDZ/immutability; public fields use observable `[[DefineOwnProperty]]`; method/accessor descriptors, arity, source text, and class `[[Call]]` rejection are certified. Direct eval inherits class grammar, `new.target`, receiver cells, and private-name identities while field-initializer `super()` remains an early error. Base construction performs resumable observable `Get(newTarget, "prototype")`; constructor-Realm fallback recursively unwraps bound/Proxy targets and throws if that Get revoked the Proxy. At pinned Test262 `be13516fb6441b950ba8a3df97eb34062c186972`, `staging/sm/class` passes 181/181, `Function/internals/Construct` passes 6/6 admitted modes (six host skips), both representative field-initializer direct-eval early-error files pass 2/2 each, and all 73 class files that failed in the supplied full report pass 146/146 focused modes; no full class-tree rate is claimed.
- [~] Private instance/static **data fields**, ordinary/generator/async/async-generator methods, and accessors have fresh opaque names and VM-only own slots; paired private getter/setter declarations share one name. Instance private methods/accessors install before any field initializer; methods are immutable, while accessors preserve receiver, reject missing or duplicate halves, and never walk prototypes or invoke Proxy traps. Reinitializing an existing private element throws. Private reads and calls participate in whole-chain optional short-circuiting without suppressing non-nullish brand checks. Generator methods retain enclosing array/object-spread state across `yield` and abandon it on abrupt `.return()`; the instance/static private-generator subtrees are 40/40 for each class form. Direct, compound, logical, prefix, postfix, destructuring, and iteration-head private writes retain one receiver/name reference; object targets evaluate their reference before the source getter as required by `KeyedDestructuringAssignmentEvaluation`. Direct-eval callsites capture the enclosing `PrivateEnvironment`; verified caller cells retain identity through frontend admission, lowering, nested eval, and escaping functions. Direct calls, `super`, bad-receiver `TypeError`, `#name in object`, nested-class capture/shadowing, and invisible/non-configurable slots are covered.
- [ ] Audit the remaining non-`eval` and eval-coupled class gaps and diagnostics. Proposal decorators remain policy-excluded.
- [~] `%eval%` uses distinct whole-graph indirect/direct Script authorities. Identity-checked direct callsites retain caller/source strictness, adjusted lexical/private scope, ordinary-call fallback, and `GetThisEnvironment` context across nested eval and arrows for `new.target`, `super`, private names, class-field `ContainsArguments`, and instance elements. Contextual `super()` binds the shared derived receiver, then invokes the constructor's verifier-certified hidden initializer; malformed headers, selectors, or ordering fail closed. Spread direct eval materializes the complete iterator but evaluates only its first value, preserving spread order and ordinary bare/`with` receiver fallback. Sloppy function eval has a per-activation variable environment shared by nested eval and escaping closures; certified parameter/callee/body boundaries preserve `arguments`, shadowing, and deletable eval-created declarations. Installation is atomic with live-cell promotion/rollback and global conflict checks. Capturable sloppy `with` environments preserve ordered reference/call/delete/write semantics, `%Symbol.unscopables%`, and nested/escaping eval lifetimes. Script `try`/`catch` resets caught completion, while each `try`/`finally` preserves the prior completion only when `finally` is normal and otherwise retains the abrupt finalizer value before `UpdateEmpty`; `language/statements/try` passes 388/388 and the staging report file 2/2. At the pinned revision, `language/eval-code` passes 400/400, `language/statements/with` 182/182, and report-failing spread eval 8/8. Strict and sloppy direct eval are both required.

### Values, objects, and built-ins

- [x] UTF-16 strings; binary64/BigInt/Symbol; canonical keys/conversions; ordinary descriptors/prototypes/integrity; functions/bound construction, including the target's observable `[[GetPrototypeOf]]` and derived `newTarget`; arrays/holes; iterator close; global lexical environments; Proxy invariants; reflection; shape/transition interning; and dense indexed storage.
- [~] Audit remaining exotic and reflection/diagnostic paths as compiler operands become reachable. Shared descriptor validation preserves non-configurable accessor identity (`Object.defineProperty` 2,250/2,250); Proxy descriptor traps cannot report or accept a frozen view of a still-writable non-configurable target, and computed/named `[[Get]]`/`[[Set]]` preserve the original receiver through nested or prototype Proxies (five reported files, 10/10 modes). Primitive `[[Get]]` likewise retains the primitive receiver after mutable intrinsic prototypes and prototype Proxies, while intrinsic lookup admits a legitimately frozen `Array.prototype.length` (two files, 4/4 report modes). Integrity statics preserve primitive/Reflect boundaries (628/628; guards 36/36); strict `Object.assign` reports anonymous-Symbol rejection as TypeError (76/76); and `Object.prototype.toString` uses only the specified builtin slots plus `@@toStringTag` (82/82). `Array.from`/`of` route `CreateDataPropertyOrThrow` through observable Proxy `[[DefineOwnProperty]]`, retaining iterator-close precedence (88/88 and 30/30 admitted modes; two host-API skips each). Mutators preserve abrupt ordering and use constant-space cursors, including `fill` across the full `ToLength` domain: `push` 48/48, `unshift` 44/44, `pop` 46/46, `shift` 40/40, `splice` 158/158 admitted (four host skips), `copyWithin` 78/78, `fill` 44/44, and `reverse` 36/36. Generic `sort` publishes through current Proxy/integer-indexed `[[Set]]` and `[[Delete]]` after observable comparisons or buffer resizes (107/107). Array iterators use `ToLength`, a full safe-integer cursor, and a fresh typed-array buffer witness on each step: `entries`/`keys`/`values` 24/24 each and `%ArrayIteratorPrototype%` 44/44 admitted (two host skips). Callback/reduction cursors likewise use ordinary decimal keys above the Array-index domain: the six report files pass 6/6, and `staging/sm/Array/to-length.js` passes 2/2. `@@unscopables` exposes the normative null-prototype table (10/10). The shared search cursor handles zero lengths, one-shot coercion, and safe-integer decimal keys: `indexOf` 401/401, `lastIndexOf` 395/395, and `includes` 60/60. `Array.prototype.toString` performs observable `join` lookup/call and intrinsic Object fallback while retaining caller origins (22/22); generic `join` snapshots `LengthOfArrayLike` before separator coercion (46/46). All runs use pinned Test262.
- [x] Globals; Object/Reflect (including `NewTarget`-aware subclass allocation, `Object.prototype.__proto__`, and the normative-optional `__defineGetter__`, `__defineSetter__`, `__lookupGetter__`, and `__lookupSetter__` methods); Error families; Boolean/Number/BigInt/Symbol; Array; JSON/Math; String; RegExp; Map/Set/weak collections; Promise; sync and async generators; and a runtime-owned FIFO job queue with resumable continuations. Legacy lookup walks observable `[[GetOwnProperty]]`/`[[GetPrototypeOf]]` methods, including a Proxy reached through an ordinary prototype fast path. Current-draft String protocol dispatch probes `@@match`, `@@matchAll`, `@@search`, `@@replace`, and `@@split` only on Objects; primitive patterns enter fallback directly (48/48 primitive modes; six subtrees 676/676). `includes`, `startsWith`, and `endsWith` apply observable `IsRegExp` after receiver conversion and before search/position coercion; their complete subtrees pass 54/54, 42/42, and 54/54, closing all 12 reported modes. The admitted legacy Function restricted accessor returns `undefined` only for zero-argument reads on sloppy constructable bytecode functions and never exposes a caller; strict/nonconstructable/native receivers and setters retain `TypeError` (22/22 report modes plus nine adjacent guards). `CreateDynamicFunction` retains generated `anonymous` source/name metadata without creating a named-expression Environment Record; direct, nested, and eval-adjacent reads resolve in the constructor Realm (`constructor-binding` 2/2; the focused Function neighborhood is 74/79, with five independent failures). RegExp String Iterator `next` converts a missing generator brand to JavaScript `TypeError` before entering its invariant-protected state machine (34/34). `RegExp.escape` rejects non-String input without coercion and uses `\x` encoding for whitespace at or below U+00FF (38/38 admitted; two host skips). Named capture and backreference identifiers cook Unicode escapes and non-Unicode UTF-16 surrogate transport before duplicate validation and lookup; `named-groups` passes 72/72 and `match-indices` 28/28, closing all 10 reported name modes. `RepeatMatcher` rejects a zero-progress optional iteration before its maximum-count exit, so attempted captures roll back while required `{1,1}` captures survive (2/2 report modes). Unicode `\c` prevalidation requires an ASCII-letter tail before any legacy class recovery (two report files, 4/4 modes). A `\q{...}` disjunction is one union-valued Unicode-set operand, preserving every alternative under direct intersection/subtraction across characters and properties of strings (14 report files, 28/28 modes). `RGI_Emoji` and `RGI_Emoji_ZWJ_Sequence` use pinned Unicode 17.0 exact fully-qualified membership; the six admitted version-delta files pass 12/12 modes without accepting minimally qualified or arbitrary ZWJ strings. `JSON.parse` reviver write-back ignores the boolean result of non-throwing `CreateDataProperty`; `Map.prototype.getOrInsertComputed` canonicalizes `-0` before callback invocation; and exact significant-digit formatting corrects power-of-ten exponent estimates before `toPrecision` rounding (6/6 report modes). `StringToBigInt` rejects signed non-decimal prefixes, and loose equality converts BigInt wrappers before cross-domain comparison (4/4 report modes). BigInt and Symbol expose `[[Construct]]` for constructor predicates and class heritage but reject a present `NewTarget` before argument coercion (8/8 report modes).
- [x] The admitted Stage 3 `Error.prototype.stack` accessor stores captured text in branded internal Error state, never an own `stack` property. Its getter rejects primitives and returns `undefined` for unbranded objects; its String-only setter implements `SetterThatIgnoresPrototypeProperties` through observable Proxy `[[GetOwnProperty]]`, `[[DefineOwnProperty]]`, and `[[Set]]` order. The pinned Test262 subtree passes all 66 admitted modes across 35 files, with four cross-realm host-API modes skipped.
- [x] `NewPromiseCapability` permits later executor calls while both captured values remain `undefined`, then validates the final callable pair. All Promise combinators share `IteratorStepValue`'s done transition: abrupt `next`, result validation, `done`, or `value` rejects without `IteratorClose`, while later element processing still closes. The 13 affected report files pass 26/26 pinned modes.
- [~] `Iterator`, `Iterator.from`, sync transforms/consumers, `%Symbol.dispose%`, `concat`, `zip`, and `zipKeyed` use retained records, rooted continuations, and normative close/error ordering. `includes` validates its Number-only skip before reading `next` and compares with SameValueZero; `join` converts its separator before looking up `next`, closes only separator/value string-conversion failures, and does not close step failures or natural exhaustion. `chunks`/`windows` validate Number-only sizes and undersized mode before reading `next`, retain GC-traced buffers, yield fresh arrays, and stop forwarding `return` after observing exhaustion. `flatMap` closes inner before outer; `concat` opens eagerly captured methods lazily; joint iteration implements all modes, reverse `IteratorCloseAll`, and null-prototype keyed results. Pinned Test262: `from` 38/38, `toArray` 36/36, `includes` 88/88, `join` 36/36, `chunks` 76/76, `windows` 80/80, `map` 72/72, `filter` 74/74, `take` 60/60, `drop` 62/62, `flatMap` 88/88, consumers 310/310, `%Symbol.dispose%` 12/12, `concat` 64/64, `zip` 76/76 at 10M fuel, and `zipKeyed` 88/88 at 12M (86/88 at 10M). Explicit-resource-management syntax and six obsolete `MAX_SAFE_INTEGER` modes remain policy-skipped; async helpers remain pending focused job-queue coverage.
- [x] The requested Annex B subset follows completed `eval`/`with`: B.1.2; B.2.2.1/.15/.16, B.2.3.1-.3, B.2.4.1, and B.3.1-.8 are complete for the core non-browser host; B.3.9 is intentionally excluded. B.1.2 admits legacy octal and non-octal decimal escapes only in sloppy string literals while preserving strict early errors (the 20 report-failing files pass 21/21); B.1.1 legacy numeric literals and B.1.3 HTML comments remain excluded. Sloppy block/switch/`if` functions retain the spec-permitted variable-environment replacement binding and all declaration/strict-mode boundaries; catch `var` declarations install in the variable environment while matching initializers and references resolve through the catch cell. B.3.5 `for-in` initializers apply `NamedEvaluation` to anonymous ordinary, generator, async, arrow, async-arrow, and class definitions before the RHS enumeration; the report-failing staging case passes 1/1. Legacy RegExp literal/eval paths retain exact UTF-16, `RegExp.prototype.compile` enforces receiver boundaries, object-literal `__proto__` emits verified `set_proto`, and the inherited `Object.prototype.__proto__` getter/setter uses observable prototype internal methods. At pinned Test262 `be13516fb6441b950ba8a3df97eb34062c186972`, focused runs pass function-code 159/159, global-code 136/136 admitted modes (17 host skips), eval-code 468/468 (two host skips), labelled 1/1, catch 10/10, `for-in` 12/12, and general catch destructuring 186/186; no merged-`annexB` rate is claimed.
- [x] Annex B.3.6 uses an irreversible embedding marker for `[[IsHTMLDDA]]`; centralized `ToBoolean`, abstract equality, and `typeof` implement exactly its three special cases. The runner's narrow `$262.IsHTMLDDA` fixture passes 68/68 admitted modes, with two async-host skips. B.3.7 retains and later ordinarily calls exact Promise reaction, thenable, and finalization callbacks, as required by the non-browser `HostMakeJobCallback`/`HostCallJobCallback` defaults; only externally ready `Atomics.waitAsync` signals enter before the next host event, while finalization cleanup remains post-event. B.3.8 needs no browser override in core: private elements install on non-host objects independently of ordinary extensibility. The browser-only hooks have no portable Test262 tranche, and the Stage 3 `nonextensible-applies-to-private` proposal remains policy-skipped. B.3.9 assignment-target paths stay excluded by a policy regression.
- [x] Date: TimeClip, normative ISO/local parsing, UTC/local getters and setters, primitive/JSON behavior, and non-Intl locale fallback over the shared `temporal_rs = 0.2.5` kernel. `Date.UTC` converts only supplied arguments and defaults an omitted month to `+0`; the affected report file passes 2/2 pinned modes. `getTimezoneOffset` canonicalizes a zero offset to `+0`, so offsetless local date-time parsing also aligns in UTC hosts; the parse subtree passes 16/16 under `TZ=UTC`.
- [x] Temporal (`temporal_rs = 0.2.5`) is complete for the admitted non-Intl runtime scope: the namespace and `Temporal.Now`; Duration, Instant, PlainDate, PlainDateTime, PlainTime, PlainMonthDay, PlainYearMonth, and ZonedDateTime; and `Date.prototype.toTemporalInstant`. Constructors, accessors, branded/string/property-bag conversion, arithmetic, comparison, field replacement, rounding/differences, calendar/time-zone projection, optioned ISO/JSON output, and primitive rejection preserve the applicable ECMA-262 ordering and validation boundaries. Observable reads and coercions use rooted resumable continuations; `Temporal.Now` uses the host UTC clock and system time zone while explicit time-zone arguments share the normative slot-value conversion path.
- [x] Pinned Test262 `be13516fb6441b950ba8a3df97eb34062c186972` passes all 9,116 admitted Temporal cases across 4,603 files with zero runner errors. The 90 policy skips are explicit: 70 baseline exclusions and 20 `Intl.Era-monthcode` modes. Focused completion evidence includes Duration 1080/1080, PlainDateTime 1502/1502, PlainTime 962/962, PlainMonthDay 398/398, PlainYearMonth 1018/1018, ZonedDateTime 1796/1796, `Temporal.Now` 132/132, and namespace metadata 6/6.
- [x] Binary data: fixed/resizable/immutable `ArrayBuffer`, fixed/growable `SharedArrayBuffer`, DataView (Float16/BigInt and fresh resizable witnesses), and the complete typed-array surface, including the six `Uint8Array` base64/base64url/hex methods with resumable option reads, exact final-chunk policies, bounded writes, and write-before-error behavior. DataView construction rechecks attachment and bounds after observable prototype lookup, preserving detached-buffer `TypeError` versus offset/length `RangeError`; its subtree passes 960/960 admitted modes (162 host-API skips). `%TypedArray%` is the constructable abstract superclass required by class heritage while its call/construct body always throws; this unblocks the shared staging harness in 43/49 report modes, with three files retaining independent timeout/fuel/`toString` failures. It alone owns the generic `@@species` getter inherited by concrete constructors (10/10 report modes). Its generic `@@toStringTag` getter returns the element-type name only for objects with `[[TypedArrayName]]` and otherwise returns `undefined`: the standard subtree passes 32/32 admitted modes (four host-API skips), the staging guard passes 2/2, and all 18 report-failing modes close. `AllocateTypedArrayBuffer` converts backing-store limit/allocation failures to catchable `RangeError`; Number and BigInt object-argument constructors pass 54/54 and 60/60 admitted modes (two host skips each). TypedArray `[[PreventExtensions]]` accepts fixed views over growable shared buffers, which cannot shrink, while rejecting length-tracking and resizable-buffer views; the six reported staging modes and 284 standard Object/Reflect integrity modes pass. `set` coerces its offset before `ToObject`, rejects only nullish sources, snapshots target length before reading source length, and takes each fresh bounds witness after value conversion; its standard subtree passes 202/202 admitted modes (18 host-API skips) and the primitive staging guard passes 2/2. `subarray` treats an initially out-of-bounds source as length zero while retaining its internal byte offset and preserves omitted-end length tracking through the species argument list; its subtree passes 128/128 admitted modes (six host-API skips). `includes` retains the validated initial length but observes indices invalidated by a later resize as `undefined`; its subtree passes 78/78 admitted modes (12 host-API skips). The distinct, branded `toLocaleString` snapshots internal length and shares only the observable element loop (74/74 admitted, four host-API skips), while `toString` is the exact Array method object (2/2). A thread-safe host handle aliases one Shared Data Block across runtimes; bytes, atomic RMW, growth, and FIFO waiter registration/notification share its critical section. Blocking waits use cross-runtime wakeups; `waitAsync` has rooted/limited waiter records, same-agent Promise-order preservation, and lazy Tokio deadline signals settled only by the runtime owner. At pinned Test262 `be13516fb6441b950ba8a3df97eb34062c186972`, focused policy runs pass `built-ins/Uint8Array` 132/132 admitted modes (8 host-API modes skipped), ArrayBuffer 408/408, Atomics 512/512 (including `waitAsync` 68/68 locally admissible; 134 async-host cases remain runner-skipped), all 42 immutable-write modes, integer-indexed `[[Set]]` 86/86 admitted (20 host-API modes skipped), and `%TypedArray%.from` 114/114 plus `of` 54/54.
- [x] ECMA-402 is isolated behind `fusor-intl` with direct ICU4X 2.2 data; JavaScript coercion and observable property order remain runtime-owned. Implemented families include Locale, Collator, NumberFormat, DateTimeFormat, PluralRules, RelativeTimeFormat, ListFormat, DisplayNames, Segmenter, DurationFormat, `supportedValuesOf`, and locale hooks for String, arrays, typed arrays, Date, and Temporal.
- [x] Before this merge, main's pinned Intl-only audit passed 6,682/6,692 standard-cap modes across 3,357 files, with 22 explicit host-API skips and zero runner errors; six residual modes passed under a focused high-fuel cap. The merged Annex B/Intl policy has not yet been rerun, so no combined-rate claim is made here.

### Modules, conformance, and host layers

- [~] Module goal compiles to whole-graph verified bytecode: a `Module` root executable kind, module-local/import/namespace bindings as captured root cells (imports immutable and TDZ-checked), `export default <expr>` stored at statement position, and a verified module declaration record (per-binding slot/mutability/init policy, import request+name descriptors, hoisted-function initializer prefixes) for the runtime linker. Source Text Module records now link and evaluate in the runtime: a per-realm registry keyed by host-canonicalized `ModuleKey`, iterative `InnerModuleLinking`/`InnerModuleEvaluation` with DFS indices and cycle-root error propagation, import cell forwarding to exporter cells, declaration initialization through the new verified `PutVarRefCheckInit` opcode, sorted live namespace exotic objects (immutable writes, `@@toStringTag` "Module"), and GC-traced records. The facade `evaluate_module` + `ModuleSourceLoader` gathers, compiles, registers, links, and evaluates the graph with phase-typed errors (18/18 focused tests). `import.meta` lowers to a verified opcode and materializes one identity-stable ordinary object per module with `url` and `resolve`, host-overridable through `ImportMetaHook`. Top-level `await` compiles the module root as an async function and evaluates per spec: `[[PendingAsyncDependencies]]`/`[[AsyncEvaluationOrder]]`/`[[AsyncParentModules]]` on the record, SCC-pop cycle roots, `evaluating-async` status, `ExecuteAsyncModule` kicking the async root, continuations through `PromiseReactionTarget::AsyncModule` (`AsyncModuleExecutionFulfilled`/`Rejected`, `GatherAvailableAncestors` in async-evaluation order), and a pending `[[TopLevelCapability]]` whose outcome hosts read through `module_evaluation_error`; `await` is now admitted in its legal syntax positions (class-heritage `super_class` expressions, `for await` loops, and module-local `var` in iteration heads and destructuring). The namespace exotic object is complete: non-extensible with a non-configurable `@@toStringTag`, string-key `[[Delete]]`/`[[DefineOwnProperty]]` rejection, and `[[Get]]`/`[[GetOwnProperty]]` throwing `ReferenceError` for exports still in their temporal dead zone. `import defer` is complete: defer-phase requests contribute only their asynchronous transitive dependencies (`GatherAsynchronousTransitiveDependencies` with cycle-root-aware `IsModuleSCCEvaluated`) to `InnerModuleEvaluation`'s evaluation list, and per-module identity-cached deferred namespaces (`[[Deferred]]`, `"Deferred Module"` `@@toStringTag`) trigger `EvaluateModuleSync` (`ReadyForSyncExecution` gate, rethrowing the stored evaluation rejection value) on every string-key access except `"then"`, across `[[Get]]`, `[[GetOwnProperty]]`, `[[HasProperty]]`, `[[DefineOwnProperty]]`, `[[Delete]]`, and `[[OwnPropertyKeys]]`. See [MODULES.md](MODULES.md).
- [~] Dynamic `import()` lowers through verified bytecode and implements `EvaluateImportCall` across the host-load boundary: each call creates an intrinsic Promise; specifier conversion, `options.with`, and enumerable import-attribute reads use rooted resumable continuations; and their abrupt completions reject instead of escaping synchronously. `HostLoadImportedModule` crosses a typed host boundary: the runtime parks a GC-rooted `PendingDynamicImport` (referrer key, specifier, attributes, Promise) bounded by `max_pending_dynamic_imports`, and the host completes it through `complete_dynamic_import`/`reject_dynamic_import` (`FinishDynamicImport`/`ContinueDynamicImport`): link failures reject with a `SyntaxError`, evaluation failures reject with the escaping exception, and success fulfills with the module namespace exotic object. The facade `pump_dynamic_imports` drives parked loads to quiescence (graph gather with registry dedup, settlement, reaction-job drain); the test262 harness pumps after module evaluation, and `language/expressions/dynamic-import` passes 499/499 admitted cases. fusor-cli drives the same parked queue on a Tokio current-thread runtime: resolution stays synchronous while the file read of each pending root crosses the await boundary, with concurrent reads per round. Dynamic import of an async-evaluating root defers settlement per `FinishDynamicImport` to the root's `[[TopLevelCapability]]`. `import.defer()` (defer-phase `EvaluateImportCall`) links the loaded graph, evaluates only the asynchronous transitive dependencies through an internal SafePerformPromiseAll (targeted reactions, never userland `Promise.prototype.then`), and fulfills with the module's cached deferred namespace object. Parsing a Module is not execution.
- [~] Embedding API, ESM REPL, `qjs`, Rust-native `qjsc`, bytecode viewer, CDP adapter, and portable `std`/`os` modules. `crates/qjs-cli` ships a `qjs` binary (`run`/`--script`/`repl`) with a Node-like `ModuleSourceLoader`: filesystem resolution (`./`/`../`/absolute, lexical normalization, `.mjs`/`.js` fallback, no directory or `node_modules` lookup) plus generated-source `node:assert`/`node:path`/`node:process` builtins. The REPL keeps one realm per session, evaluates non-module entries as scripts, and prepends accumulated single-line imports to module entries — documented host sugar, not a spec module record. Dependency resolution is host-driven: the host records a (referrer, specifier) → module edge per load (`register_module_dependency`), the linker reads only those edges (ECMA-262 `HostResolveImportedModule`), and loader-issued keys are canonical (`file://` paths, `node:` builtins), so one file under two specifier texts dedups and identical specifier text from two referrers resolves independently. The whole CLI runs on one Tokio current-thread runtime: the static graph is gathered level by level with concurrent `tokio::fs` reads (resolution stays synchronous; only file reads cross the await boundary) and handed to the facade as preloaded edges (`module_import_requests`/`evaluate_preloaded_module_graph`; the sync `evaluate_module` now composes the same path, harness behavior unchanged), and REPL module entries take the same async path. Host-function installation (`print`) does not exist yet.
- [x] The release-mode Test262 runner and manual-dispatch Ubuntu GitHub Actions workflow clone current upstream Test262, apply the custom feature/path policy with narrow completed-Annex-B overrides (and `Temporal` enabled), use explicitly sized bounded Rayon workers with 64 MiB stacks and a Tokio progress coordinator, cap CI at four case workers to bound memory, enforce an uncatchable five-second deadline per case plus a one-hour CI ceiling, and give each isolated Test262 realm 65,536 dynamic compilations while the engine default remains 1,024. It rejects impractically slow unbounded debug runs, streams flushed aggregate progress and the live pass rate every 1,000 completed cases, prints the revision/full-suite pass rate, and uploads a deterministic JSON report. Typed negative results classify intrinsic Errors without observable property access and recognize the harness-defined `Test262Error` through a bounded closure over its original prototype; the seven affected report files pass 14/14 modes even if test code rebinds the constructor. Module-flagged tests now execute as modules through `evaluate_module` with a filesystem loader confined to the suite root (loader/link failures classify as resolution-phase `SyntaxError`, evaluation failures as typed runtime exceptions) followed by `pump_dynamic_imports` for parked `import()` loads; the focused `language/module-code` run admits 597 cases and passes 596 (99.83%), with arbitrary string export names, JSON/text module sources (`with { type: "json"|"text" }`), async-host `$DONE`/`print` completion, and `import defer` (static and dynamic `import.defer()`; `language/import` passes 129/129 with the feature admitted) all supported; the one remaining failure is a cyclic-import module-function identity edge case (a root-module function declaration under a cyclic import yields distinct function objects for the module binding, the import binding, and the function's self-name, so `verify-dfs.js`'s shared-function-array trick never sees the importer's writes), and `source-phase-imports` (5) remains deferred; arrow functions, module-level `class` declarations, anonymous default class/function/generator exports (hoisted and named `"default"`), and the legal top-level-await syntax positions compile in module units. Module evaluation carries the dynamic-function compiler, so `eval`/`Function` work inside module code (`eval-gtbndng-*`, `eval-rqstd-*`, and `language/expressions/import.meta` 27/27). `language/expressions/dynamic-import` passes 1442/1444 admitted cases (async-host tests and dynamic `import()` `with { type: "json"|"text" }` attributes run; a requested module that fails to parse or compile now rejects the import with a `SyntaxError`, and `Evaluate()` redirects an evaluated member of an errored cycle to its cycle root's recorded `[[EvaluationError]]`; the two residual failures are one generator + `yield`-in-import-options compiler case in both modes). ResolveExport is a tri-state (resolved/null/ambiguous): star diamonds reaching one binding dedup, namespace enumeration omits ambiguous names, `export * as name` resolves to the target namespace object (self-references and cycles included), local re-exports of imported bindings lower to indirect entries, and every indirect export entry is validated at link time; `ambiguous-export-bindings` passes 9/9. Star re-export cycles follow `GetExportedNames`'s `exportStarSet`, so self/circular `export *` graphs link instead of overflowing the worker stack.
- [~] Full-suite baseline: CI run `31381832450`, artifact `9060591368`, engine `eb131f37ff44a08f5476911888c3dff2adc34a42`, pinned Test262 `be13516fb6441b950ba8a3df97eb34062c186972`, and policy `f961b24d71d333c1` pass 79,070/79,779 admitted modes (99.11%) with zero runner errors. The report accounts for 709 failures across 449 files, 23,133 explicit skips, 10 50M-fuel exhaustions, and 53,575 inventoried files. It includes the prior focused fixes through Proxy descriptor/receiver alignment and predates these later cohorts: Function restricted accessors (22 report modes), parameter-environment `arguments` shadowed by body lexicals (4), shorthand destructuring class `NamedEvaluation` (4), Annex B.3.5 `for-in` initializer `NamedEvaluation` (1), simple identifier Reference retention (3), TryStatement completion propagation (4), dynamic-Function synthetic-name binding removal (2), PTC Error stacks (34), `%TypedArray%` constructor/tag/species (71), RegExp iterator/escape/named-capture/repeat/Unicode escape/set-string/RGI alignment (62), mutable intrinsic prototypes/descriptors (4), TypedArray `set`/`subarray`/`includes`/string methods/allocation (44), DataView range classification (4), Date signed zero (2), revoked-Proxy constructor Realm (2), growable-shared TypedArray integrity (6), String protocol/`IsRegExp` alignment (58), computed member PropertyKey retention (22), captured compound-identifier Reference retention (11), and runner `Test262Error` recognition (14). No later full run includes these fixes, so 99.11% remains the only full-suite claim.
- [ ] Drive each admitted failure cohort against ECMA-262 and QuickJS/Node, remove temporary skips as features land, and rerun the full configured suite after the language/compiler/module gates close.
- [ ] Establish startup/memory/interpreter/compile benchmarks and finish release, resource, cancellation, dependency, and public-API audits.

## Compatibility differences

| ID | Intentional behavior |
| --- | --- |
| `FUS-OXC-001` | Oxc determines RegExp literal boundaries/flags; the owned RegExp layer owns grammar, early errors, and execution. |
| `FUS-OXC-002` | A post-semantic check rejects one chained-label `continue` target accepted by Oxc but rejected by QuickJS. |
| `FUS-OXC-003` | This frontend uses an independent bounded nesting stack; QuickJS's near-695-parenthesis overflow is non-normative. |
| `FUS-OXC-004` | Instance field `prototype` follows ECMA-262; QuickJS rejects it although only static fields reserve it. |
| `FUS-OXC-005/006` | Annex B HTML comments (B.1.3) remain excluded from ordinary and host-forced strict Script profiles. |
| `FUS-BIGINT-001` | `BigInt.asUintN` reduces modulo `2**bits`, including negative inputs. |
| `FUS-PROMISE-001` | Hostile synchronous `then` calls share the required `[[AlreadyCalled]]` record in `Promise.allSettled`. |
| `FUS-ASYNC-GENERATOR-001` | Handled async `yield*` `.return()` preserves a thenable value property rather than assimilating it. |
| `FUS-MAP-001` | `getOrInsertComputed` rescans and updates a callback-created key in place rather than deleting/re-appending it. |
| `FUS-TEMPLATE-001` | Untagged templates use intrinsic concatenation with immediate `ToString`, not observable `String.prototype.concat`. |
| `FUS-REGEXP-001` | Unicode-set string disjunction in lookbehind follows ECMA-262/Node rather than QuickJS rejection. |
| `FUS-REGEXP-002` | `RegExp.escape` retains non-whitespace Unicode scalars rather than hex-escaping them. |

## Completion gates and engineering rules

During development, run only changed-package and focused integration tests; do not repeatedly run the workspace suite. Before release, a full-conformance claim, or goal completion, run:

```console
cargo fmt --all -- --check
cargo clippy --workspace --all-targets --all-features -- -D warnings
cargo test --workspace --all-features
RUSTDOCFLAGS="-D warnings" cargo doc --workspace --all-features --no-deps
cargo audit
```

1. Follow ECMA-262 first; document intentional differences.
2. Reject unsupported semantics rather than approximating them; execute only whole-graph verified bytecode.
3. Keep parser, compiler, verifier, VM, built-in, and host ownership separate.
4. Use explicit worklists and typed continuations; Rust recursion, locks, and Tokio must not define JavaScript behavior.
5. Runtime/context/heap/JS handles are thread-affine and `!Send + !Sync`.
6. Keep the core safe; foreign pointers belong only to an audited boundary.
7. Performance changes require profiles and differential evidence; `unsafe` is never an optimization escape. Proper tail calls retain their language-semantics gate above; shared-memory scheduling must preserve the runtime-owned waiter and Promise-job order.
